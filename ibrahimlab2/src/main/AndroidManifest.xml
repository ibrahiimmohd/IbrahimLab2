<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ibrahim.ali.s301022172">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/ibrahimAppTitle"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".aliActivityFinalOrder"></activity>
        <activity android:name=".aliActivityPayment" />
        <activity android:name=".aliActivityCheckout" />
        <activity android:name=".aliActivityHomeTypes" />
        <activity
            android:name=".ibrahimActivity"
            android:label="@string/ibrahimAppTitle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>